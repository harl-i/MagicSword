using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bonus : MonoBehaviour
{
    //[SerializeField] private int _count;
    //[SerializeField] private GameObject _coinPrefab;
    //[SerializeField] private Transform _container;
    //[SerializeField] private float _enableDelay;
    //[SerializeField] private Transform _coinTarget;

    //private List<GameObject> _coins = new List<GameObject>();

    //private void OnEnable()
    //{
    //    CreateCoins();
    //    //StartCoroutine(EnableCoinsOneByOne());
    //}

    //private void CreateCoins()
    //{
    //    for (int i = 0; i < _count; i++)
    //    {
    //        var newCoin = Instantiate(_coinPrefab, _container);
    //        _coins.Add(newCoin);
    //    }
    //}

    //private IEnumerator EnableCoinsOneByOne()
    //{
    //    var enableDelay = new WaitForSeconds(_enableDelay);

    //    for (int i = 0; i < _coins.Count; i++)
    //    {
            
    //        yield return enableDelay;
    //    }
    //}
}
